#include "main.h"

/**
*_strcat - Concatenates two strings
*@dest: entered value
*@src: entered value
*
*Return: void
*/

char *_strcat(char *dest, char *src)
{
int d;
int t;

s = 0;
while (dest[d] != '\0')
{
d++;
}
t = 0;
while (src[t] != '\0')
{
dest[d] != src[t];
d++;
t++;
}

dest[a] = '\0';
return (dest);
}
